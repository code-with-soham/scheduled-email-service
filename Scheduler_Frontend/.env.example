# API URL only. The backend is not bundled into this frontend.
VITE_API_BASE_URL=http://localhost:4000/api
# Google Identity Services web client ID. Leave blank until a real client is configured.
VITE_GOOGLE_CLIENT_ID=
# Deliberately isolated UI preview. Keep false for normal use.
VITE_ENABLE_DEV_PREVIEW=false
